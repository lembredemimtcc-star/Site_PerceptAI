# Project Memory

## Core
App PerceptAI (monitoramento hospitalar): cores em src/config/colors.ts (laranja #F2652E), fonte Inter, UI em src/modules/*.
Preferências de acessibilidade ficam em localStorage ("perceptai:acessibilidade") — sem backend.
Sem VITE_SUPABASE_URL/ANON_KEY o app roda em modo demonstração (login ignorado, dados mock).
