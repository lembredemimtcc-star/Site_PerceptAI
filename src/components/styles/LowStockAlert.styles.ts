import { COLORS } from "../../config/colors";

export const lowStockAlertStyles = {
  alertBox: {
    background: COLORS.redSoft,
    borderColor: COLORS.red,
  },
  alertIconColor: COLORS.red,
  alertTitle: {
    color: COLORS.red,
  },
  alertSubtitle: {
    color: COLORS.red,
  },
};